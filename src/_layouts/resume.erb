<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
    <link rel="stylesheet" href="/_bridgetown/static/resume.css" data-turbo-track="reload" />
  </head>
  <body class="<%= data.layout %> <%= data.page_class %>">
    <%= yield %>
  </body>
</html>
